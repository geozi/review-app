package gr.aueb.cf.reviewapp.model;

/**
 * The {@link Role} enum class contains
 * the standardized options of the Role field
 * in the {@link User} class.
 */
public enum Role {
    ADMIN,
    USER
}
